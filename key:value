#include<bits/stdc++.h>
using namespace std;
int main()
{
   vector<int>v={1,2,1,2,1,3,2,};
    map<int,int>m;
    for(int i=0;i<v.size();i++)
    {
      m[v[i]]++;  
    }
    for(auto it:m){
        cout<<"key "<<it.first<<":"<<it.second<<"repeating"<<endl;
    }
    return 0;
}
